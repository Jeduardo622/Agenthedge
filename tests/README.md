# Tests

This folder will contain:

- Unit tests for data pipelines, risk/compliance logic, and execution adapters.
- Integration tests exercising the multi-agent orchestration loop.
- Fixtures for deterministic market data snapshots.

Run all tests via `pytest` once we add modules.
